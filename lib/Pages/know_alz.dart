import 'package:flutter/material.dart';

class KnowAlzPage extends StatelessWidget {
  const KnowAlzPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Know Alzheimer")),
    );
  }
}